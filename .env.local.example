# Copy this file to .env.local (which is gitignored) and fill in real values.
# .env.local is populated automatically by `npx convex dev` on first run —
# that command requires an interactive browser login to your Convex account,
# so it must be run by a human, not this agent. See README.md "Getting Started".

VITE_CONVEX_URL=
